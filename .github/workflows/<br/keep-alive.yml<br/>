name: Keep Supabase Alive<br/><br/>on:<br/>  schedule:<br/>    - cron: '0 9 */5 * *'  # 每5天执行一次<br/>  workflow_dispatch:        # 允许手动触发<br/><br/>jobs:<br/>  ping:<br/>    runs-on: ubuntu-latest<br/>    steps:<br/>      - name: Ping Supabase Database<br/>        run: |<br/>          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" \<br/>            "${{ secrets.SUPABASE_URL }}/rest/v1/compounds?select=id&limit=1" \<br/>            -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" \<br/>            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}")<br/>          echo "Response code: $RESPONSE"<br/>          if [ "$RESPONSE" != "200" ]; then<br/>            echo "Warning: unexpected response $RESPONSE"<br/>            exit 1<br/>          fi<br/>
未选择文件
